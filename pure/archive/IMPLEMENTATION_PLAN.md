# IMPLEMENTATION PLAN — seal the base, then open the frontier

Two parts. Part A finalizes what is built (executable now, fully gated). Part B is the emergence frontier — open work, built in the permitted language, gate-enforced, with results reported as the system produces them, never claimed as physics.

---

## PART A — Lock in the base (the immediate mandate)

**A1 — Canonize opposition.** Add two theorems to `claims_pure.py`, each with an exact confirmation built only on `ratio.py`:
- **Q12 / R9:** `reciprocal(r) · r = the One` — the opposite of a relation times the relation returns the One (multiplicative balance replacing additive zero).
- **Q13 / R10:** `separation(p, antipode(p)) = the half-One` — a position and its opposite are maximally apart; balance of equal opposites returns the One.
Write R9, R10 in `PROOFS_pure.md` (both prove directly from the definitions, quantified over all r, all p).

**A2 — Scale to the ceiling.** Add both to `stress_pure.py`: run in exact rationals to the machine's exact-arithmetic ceiling (reciprocal product over r with numerators/denominators into the thousands of bits; antipode separation over p across deep dyadic and non-dyadic parts), recording the largest scale with zero failures.

**A3 — Green the gate.** Fold Q12, Q13 into `coverage_pure.py` and the master `GATES_pure.py`. Re-run until the master gate reports ALL CLEAN. At that point the base is closed: every claim proven, quantified, scaled, and the no-apparatus LAW verified — opposition handled with no negatives and no zero.

*Outcome:* the base system is officially closed and proven. Mechanical; ready to execute.

---

## PART B — The emergence frontier (open research)

**Discipline for every step (non-negotiable, per the LAW):** built in ratio / no-negative / no-zero / no-imaginary terms, `no_apparatus_gate.py` enforced; exact where the result is exact; each finding tagged **THM** (proved) or **OBS** (measured, parameter-specific) — never an OBS dressed as a theorem. The system is sovereign: results are reported as it produces them. Any resemblance to a physical phenomenon (a wave, a fringe, a beat) is **noted as the system's output, never used to validate it, and never claimed as established physics.** What the patterns mean against observation is for you and your professor to assess; the artifacts show only what the system does.

**B1 — Density over amplitude.** Build an ensemble of many folds on the whole. Define the observable as a **density of states** — exact counts of how many ones occupy each region. The opposition/cancellation state (relations returning to the One) marks regions that thin to a "gap"; others cluster and compound. *Deliverable:* `emergence_density.py` — the measured density profile as exact counts, tagged OBS; any exact structure (e.g. a provable thinning at a region) tagged THM.

**B2 — Rational harmonics / beats.** Run two or more folds with different starting parts and/or fold factors. Because exact rationals are eventually periodic, their interlocking cycles produce macro-periods. *Deliverable:* `emergence_beats.py` — the combined period computed **exactly** (the cycle is the least common multiple of the component cycle-lengths: this part is a THM, provable and exact), and the beat structure measured (OBS). Wave-like macro-pattern from interlocking rational rhythms, with no continuous curve and no π.

**B3 — Aggregate rule (cellular).** Many ones under the coupled fold (pull toward unison, threshold (m−1)/m from `coupling.py`), evolved together. Record the macro-structure that emerges from the simple deterministic local rule. *Deliverable:* `emergence_aggregate.py` — the measured aggregate structure across ensemble size and coupling, tagged OBS; any size-independent invariant that proves out tagged THM.

**B-gate.** A separate emergence registry and gate: every B-result is either proven (THM, in `PROOFS_pure.md`) or labeled OBS with its exact parameters; the no-apparatus gate runs over all B-modules; the emergence gate is green only when nothing is unlabeled and nothing forbidden appears.

---

## Order
1. A1 → A2 → A3 — green the master gate; base closed.
2. B1, B2, B3 as separate gated modules, each result tagged THM/OBS.
3. Re-run the full master gate (base + emergence): green only when the base is proven, every emergence result is proven or honestly labeled, and no forbidden construct appears anywhere.

## What "done" will and will not mean
- **Will:** the base is proven, closed, scaled, and gated in your language; the emergence experiments are built, run in pure terms, and reported with exact results, honestly tagged.
- **Will not (and I will not claim):** that the emergence patterns reproduce or explain physical phenomena. Whether what the system produces corresponds to waves, interference, or orbits is an empirical question you and your professor judge against observation — the artifacts establish what the system does, not that it matches the world. That boundary is held so the eventual "yes" on the base stays true, and the frontier stays honest open work.
