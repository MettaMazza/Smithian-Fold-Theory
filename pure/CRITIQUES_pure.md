# CRITIQUES (permitted language) — classes and closures

- **Forbidden apparatus (§8)** — the decisive class. Closed: the whole system is rebuilt from the One in ratio/division only; `no_apparatus_gate.py` fails on any imaginary, negative literal, zero-as-value, sine/cosine, pi, or subtraction outside the two audited primitives (`cast_out`, `take`). Verified to bite on a probe and to pass the corpus.
- **Model substituted for the system** — closed: there is no complex/signed model here; positions, fold, relation, separation, coupling are all parts-of-the-One and ratios. The gate enforces it.
- **Finite-sample-not-proof** — closed: `PROOFS_pure.md` derives every theorem from the One, quantified; confirmations are exact rationals.
- **Untested scale** — closed: `stress_pure.py` runs exact rationals to the machine ceiling (k≈2.67M, bit-length 24k, M=262144, m=200000), zero failures.
- **Apparatus-induced constants** — surfaced and resolved: the band width 1/(2π) was an artifact of the sine/radian apparatus; the system's own holding threshold is the ratio (m−1)/m, no π. Recorded as the system's result.
- **Claim omission** — closed: `fold_system_pure.py` iterates the registry directly; no claim can be silently dropped.

Open line for any class the author adds.

## Part B additions
- **Gate that scanned nothing (caught and fixed)** — the no-apparatus gate globbed `pure/*.py` while run from inside `pure/`, matching zero files and reporting CLEAN vacuously. Rebuilt on the AST: it now scans by absolute directory, distinguishes a forbidden construct from scaffolding (string text, list indices `a[i-1]`, loop timers `time()-t0`) by structure, and is verified to bite on a probe (`-5`, `a-b`, `cmath`, `complex`, `3j`). Three genuine apparatus leaks it had hidden (a float-difference comparison; two integer subtractions building magnitudes) were fixed in the permitted language.
- **Counts vs magnitudes** — integer bookkeeping (periods, region indices, ensemble sizes, loop counters) are scaffolding, not magnitudes; the system's magnitudes remain strictly positive parts of the One, with no zero or negative. The gate enforces the latter.
- **Emergence results tagged honestly** — E1 (beat period = lcm) and E2 (dyadic lattice collapses to the One) are proved (RB1, RB2); E3 (odd lattice spreads) and E4 (clustering collapses at the half-One) are measured observations, labelled OBS, not theorems.
- **No physics claim** — the emergence modules report what the system produces (density profiles, beat periods, clustering). Whether these correspond to physical phenomena is not asserted and is not established by these artifacts.
