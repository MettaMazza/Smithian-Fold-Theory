# PV12 dev — "The Mirror and the Hinge" (antimatter / why something not nothing / CP; pairs Ep 13)

*(Dev clutter + post description copy here. Script file = clean spoken prose only.)*

## Truth gate (re-run 2026-06-17)
| Element | Detail | Anchor |
|---|---|---|
| The Mirror | every value has a complement; `take(ONE, x) = 1 − x`; the pair sums to exactly ONE (1/3 + 2/3 = 1, on the nose) | take identity; `verify_cp_phase_antipode` |
| Annihilation | matter+antimatter = a value and its Mirror folding back to the One | (Mirror identity) |
| Why something survived | complete annihilation = the sum reaching **zero**; zero is forbidden (No-Zero floor) → a residue is left, proven nonzero = **½** | `verify_baryon_asymmetry_nonzero` |
| The hinge (CP) | CP-violating phase = the **Still Point ½**, the unique self-antipode (its own Mirror), point of maximal violation | `verify_cp_phase_antipode` |
| Jarlskog | fold **3.13×10⁻⁵** vs measured **3.08×10⁻⁵** (≈1 part in 70) | `verify_cp_phase_antipode` |
| Baryon-to-photon | CP-measure² × half-One imbalance ≈ **4.9×10⁻¹⁰** vs measured order ~**6×10⁻¹⁰** | `verify_baryon_to_photon_ratio` |
| Strong-CP | opposition lands cleanly at the One — **no** strong-CP fine-tuning problem | `verify_strong_cp_alignment` |
| Trace to One | `verify_value`; zero free parameters | — |

**Framing guard:** the *structure* (Mirror = take(ONE,·); residue forced by No-Zero; hinge forced to ½) is forward-forced; the *match* to measured CP violation and baryon asymmetry is the external read. NO postdiction/ranking, NO caveat. "Why is there something rather than nothing" answered with two rules already on the table (no-nothing + the self-mirror half).

## Gates
- **Truth gate ☑** — Mirror pairs sum to ONE; residue nonzero ½ (`verify_baryon_asymmetry_nonzero`); CP phase = self-antipode ½ maximal, Jarlskog 3.13e-5 vs 3.08e-5 (`verify_cp_phase_antipode`); baryon-to-photon ≈4.9e-10 vs ~6e-10 (`verify_baryon_to_photon_ratio`); strong-CP aligned (`verify_strong_cp_alignment`); forward-forced; zero free parameters.
- **Proof-register gate ☑** — technical; the Mirror identity; the No-Zero residue mechanism (same floor as the black-hole singularity); the ½ hinge; anchors named; the "two rules you already had" close; AI-image disclaimer near close; CTA → *The Unfolding Adventures*; description = repo + Zenodo. `voice_gate.py` → pass.

## Post description copy (NOT spoken)
> Antimatter is the Mirror: every value's complement, `take(ONE, x) = 1 − x`, the pair summing to exactly the One (`verify_cp_phase_antipode`). Annihilation is a value and its Mirror folding home. Why didn't everything cancel to nothing? Because zero is forbidden — the No-Zero floor leaves a residue, proven nonzero and equal to ½ (`verify_baryon_asymmetry_nonzero`). The hinge that makes *matter* the survivor is the Still Point ½, the unique self-antipode, maximal CP violation: Jarlskog 3.13×10⁻⁵ vs measured 3.08×10⁻⁵; baryon-to-photon ≈4.9×10⁻¹⁰ vs ~6×10⁻¹⁰. Strong-CP aligns cleanly at the One — no fine-tuning. Zero free parameters.
>
> • Code: https://github.com/MettaMazza/Smithian-Fold-Theory — run `verify_baryon_asymmetry_nonzero` and `verify_cp_phase_antipode`.
> • Papers (Zenodo): https://doi.org/10.5281/zenodo.20515256
>
> The story version → *The Unfolding Adventures.*

## Runtime
~800 words ≈ ~5 min (technical pace). Clean spoken-prose file confirmed.
